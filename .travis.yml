language: java
cache:
  directories:
  - "$HOME/.m2"
sudo: required
notifications:
  email:
  - ci@estimator.cloud
  on_success: always
  on_failure: always
branches:
  only:
  - master
install: true
script: ./mvnw install dockerfile:build
